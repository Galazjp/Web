<main role="main">
      <section class="jumbotron text-center">
        <div class="container">
          <h1 class="jumbotron-heading">Types</h1>
        </div>
      </section>
      <div class="py-5 bg-light">
        <div class="container">
          <div class="row">
            <% @types.each do |type| %>
              <div class="col-md-4">
                <div class="card mb-4 shadow-sm">
                  <div class="card-body">
                    <h5 class="card-title"><%= type.name %></h5>
                    <p class="card-text"><td><%= type.description %></td></p>
                    <div class="d-flex justify-content-between align-items-center">
                      <div class="btn-group">
                        <%= link_to 'Pets', type, class:"btn btn-sm btn-outline-primary"%>
                        <% if user_signed_in?%>
                          <%= link_to 'Edit', edit_type_path(type), class:"btn btn-sm btn-outline-secondary"%>
                          <%= link_to 'Delete', type, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-sm btn-outline-secondary" %>
                        <% end %>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>
</main>

<footer class="text-muted">
    <div class="container">
      <p class="float-right">
        <%= link_to 'New Type', new_type_path, class:"btn btn-primary my-2"%>
      </p>
    </div>
</footer>